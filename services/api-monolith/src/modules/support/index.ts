/**
 * Support module — Phase 3 migration stub.
 * No inter-service HTTP calls. Standalone Mongoose model + routes.
 * Trivial migration: copy index.ts models + routes, no event wiring needed.
 * Port to migrate from: 8015
 */
export {};

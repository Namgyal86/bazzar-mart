/**
 * Storefront Designer module — Phase 3 migration stub.
 * No inter-service HTTP calls. Standalone Mongoose model + routes.
 * After migration: sellers module can import Storefront model directly
 * to embed storefront config in seller dashboard responses.
 * Port to migrate from: 8011
 */
export {};

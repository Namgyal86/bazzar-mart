# =============================================================
# Bazzar — Notification Service Kubernetes Manifests
# Handles: In-app notifications, email (SendGrid), SMS (Sparrow), push (Firebase)
# Port: 8008
# =============================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: bazzar
  labels:
    app: notification-service
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/part-of: bazzar
    app.kubernetes.io/version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: notification-service
        app.kubernetes.io/name: notification-service
        app.kubernetes.io/part-of: bazzar
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: notification-service
          image: ghcr.io/your-org/bazzar/notification-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8008
              protocol: TCP
          envFrom:
            - configMapRef:
                name: bazzar-config
            - secretRef:
                name: bazzar-secrets
            - secretRef:
                name: bazzar-notification-secrets
          env:
            - name: PORT
              value: "8008"
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: bazzar-db-secrets
                  key: NOTIFICATION_SERVICE_MONGO_URI
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
      imagePullSecrets:
        - name: ghcr-pull-secret

---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: bazzar
  labels:
    app: notification-service
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/part-of: bazzar
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8008
      targetPort: 8008
      protocol: TCP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification-service-hpa
  namespace: bazzar
  labels:
    app: notification-service
    app.kubernetes.io/part-of: bazzar
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80

# =============================================================
# Bazzar — Storefront Designer Service Kubernetes Manifests
# Handles: Custom seller storefronts, S3/CloudFront assets
# Port: 8011
# =============================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: storefront-designer-service
  namespace: bazzar
  labels:
    app: storefront-designer-service
    app.kubernetes.io/name: storefront-designer-service
    app.kubernetes.io/part-of: bazzar
    app.kubernetes.io/version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: storefront-designer-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: storefront-designer-service
        app.kubernetes.io/name: storefront-designer-service
        app.kubernetes.io/part-of: bazzar
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: storefront-designer-service
          image: ghcr.io/your-org/bazzar/storefront-designer-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8011
              protocol: TCP
          envFrom:
            - configMapRef:
                name: bazzar-config
            - secretRef:
                name: bazzar-secrets
          env:
            - name: PORT
              value: "8011"
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: bazzar-db-secrets
                  key: STOREFRONT_SERVICE_MONGO_URI
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 400m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8011
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8011
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
      imagePullSecrets:
        - name: ghcr-pull-secret

---
apiVersion: v1
kind: Service
metadata:
  name: storefront-designer-service
  namespace: bazzar
  labels:
    app: storefront-designer-service
    app.kubernetes.io/name: storefront-designer-service
    app.kubernetes.io/part-of: bazzar
spec:
  type: ClusterIP
  selector:
    app: storefront-designer-service
  ports:
    - name: http
      port: 8011
      targetPort: 8011
      protocol: TCP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: storefront-designer-service-hpa
  namespace: bazzar
  labels:
    app: storefront-designer-service
    app.kubernetes.io/part-of: bazzar
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: storefront-designer-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80

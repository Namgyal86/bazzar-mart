# =============================================================
# Bazzar — Seller Service Kubernetes Manifests
# Handles: Seller profiles, dashboard, approval workflow
# Port: 8007
# =============================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: seller-service
  namespace: bazzar
  labels:
    app: seller-service
    app.kubernetes.io/name: seller-service
    app.kubernetes.io/part-of: bazzar
    app.kubernetes.io/version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: seller-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: seller-service
        app.kubernetes.io/name: seller-service
        app.kubernetes.io/part-of: bazzar
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: seller-service
          image: ghcr.io/your-org/bazzar/seller-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8007
              protocol: TCP
          envFrom:
            - configMapRef:
                name: bazzar-config
            - secretRef:
                name: bazzar-secrets
          env:
            - name: PORT
              value: "8007"
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: bazzar-db-secrets
                  key: SELLER_SERVICE_MONGO_URI
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8007
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8007
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
      imagePullSecrets:
        - name: ghcr-pull-secret

---
apiVersion: v1
kind: Service
metadata:
  name: seller-service
  namespace: bazzar
  labels:
    app: seller-service
    app.kubernetes.io/name: seller-service
    app.kubernetes.io/part-of: bazzar
spec:
  type: ClusterIP
  selector:
    app: seller-service
  ports:
    - name: http
      port: 8007
      targetPort: 8007
      protocol: TCP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: seller-service-hpa
  namespace: bazzar
  labels:
    app: seller-service
    app.kubernetes.io/part-of: bazzar
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: seller-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
